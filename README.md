## Node application to provide REST APIs

To run this application locally, you will need to provide a MongoDB url in the config.env file.

Then you will need to run npm install

Then you can run npm run dev
